import { createContext } from "vm";

// .
const REDUCER_ATION_TYPE = {
    ADD: "ADD",
};

// .
const reducer = () => {};

// . Data
const useImageContext = () => {};

// . 
const ImageContext = createContext();

// . Create the data provider component
const ImageContextProvider = () => {};
